const compProps = ['cssModule', 'configuration', 'staticData', 'componentId', 'parentId']

interface compPropsInter {
  cssModule: any
  configuration: any
  staticData: any
  componentId: string
  parentId: string
}
export { compProps, compPropsInter }
